---
// Required by Astro's i18n routing: with `prefixDefaultLocale: true`,
// there's no unprefixed page of our own at "/" (both locales live under
// /en/ and /tr/), but Astro still needs an index page to exist here in
// order to inject the `redirectToDefaultLocale` redirect to `/en/`. See
// astro.config.mjs and CLAUDE.md's note that `defaultLocale: 'en'` is a
// low-stakes technical fallback only, not a "primary language" decision.
---
